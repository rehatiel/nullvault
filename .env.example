# ── Required ────────────────────────────────────────────────────────────────

# Publicly accessible base URL — no trailing slash
BASE_URL=https://your-domain.com

# Secret key required in X-Creation-Secret header to create new links
# Generate with: openssl rand -hex 32
CREATION_SECRET=change-me-to-a-long-random-string


# ── Branding (optional) ──────────────────────────────────────────────────────

# Display name shown in the nav bar, footer, and page titles
# Default: NullVault
SITE_NAME=NullVault

# Logo character or emoji shown in the nav bar icon
# Default: 🔒
SITE_LOGO=🔒


# ── Optional (defaults shown) ────────────────────────────────────────────────

# Port the server listens on inside the container
PORT=3000

# Number of Express reverse-proxy hops to trust for IP forwarding
# Set to 1 when behind a single proxy (Nginx, Caddy, Cloudflare, etc.)
TRUST_PROXY=1

# Default link lifetime in days (0 = never expires)
DEFAULT_EXPIRY_DAYS=30

# How many days to retain access logs before auto-deletion
RETENTION_DAYS=30

# Rate limiting — window in milliseconds and max requests per window per IP
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=20

# SQLite database path inside the container (mapped to a named volume)
DB_PATH=/app/data/honeypot.db
